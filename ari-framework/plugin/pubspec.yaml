name: ari_plugin
description: Shared ARI bridge package for Flutter desktop apps.
version: 0.2.5
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  provider: ^6.1.5+1
  flutter_markdown: ^0.7.7+1
  http: ^1.6.0
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
