name: ari_widgets
description: ARI framework UI components — reusable, customizable Flutter chat widgets.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  flutter_markdown: ^0.7.7+1
  ari_plugin:
    path: ../plugin

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
